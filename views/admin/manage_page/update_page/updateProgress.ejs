<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Cập nhật bài viết</title>
  <!-- MDBootstrap -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.2.0/mdb.min.css" rel="stylesheet"/>
</head>
<body class="bg-light">

  <!-- Container -->
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <!-- Card -->
        <div class="card shadow-lg border-0 rounded-4">
          <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
            <h4 class="mb-0">✏️ Cập nhật tài khoản</h4>
          </div>
          <div class="card-body p-4">
            <form action="/dashboard-tien-trinh/<%= progressData.id %>/cap-nhat" method="POST">
              <!-- User_id -->
              <div class="mb-3">
                <label for="user_id" class="form-label">User_id</label>
                <input type="text" value="<%= progressData.user_id %>" id="user_id" name="user_id" class="form-control" placeholder="Nhập tiêu đề bài viết" value="User_id">
              </div>

              <!-- Tiêu đề -->
              <div class="mb-3">
                <label for="title" class="form-label">Tiêu đề</label>
                <input type="text" value="<%= progressData.title_progress %>" id="title" name="title" class="form-control" placeholder="Nhập tiêu đề bài viết" value="Tiêu đề cũ">
              </div>

              <!-- Mô tả -->
              <div class="mb-3">
                <label for="description" class="form-label">Mô tả</label>
                <input type="text" value="<%= progressData.description_progress %>"  id="description" name="description" class="form-control" placeholder="Nhập mô tả ngắn" value="Mô tả cũ">
              </div>

              <!-- Phần trăm hoàn thành -->
              <div class="mb-3">
                <label for="percent" class="form-label">Phần trăm hoàn thành</label>
                <input type="text" value=""  id="percent" name="percent" class="form-control" placeholder="Nhập mô tả ngắn" value="Xác nhận mật khẩu">
              </div>

              <!-- Hạn cuối -->
              <div class="mb-3">
                <label for="percent" class="form-label">Hạn cuối</label>
                <input type="text" value=""  id="deadline" name="deadline" class="form-control" placeholder="Nhập mô tả ngắn" value="Xác nhận mật khẩu">
              </div>

              <!-- Trạng thái -->
              <div class="mb-3">
                <label for="status" class="form-label">Trạng thái</label>
                <select id="status" name="status" class="form-control" required>
                    <option value="0">Chưa làm</option>
                    <option value="1">Đang làm</option>
                    <option value="2">Đã hoàn thành</option>
                </select>
              </div>

              <!-- Buttons -->
              <div class="d-flex justify-content-between">
                <a href="/posts" class="btn btn-secondary">⬅ Quay lại</a>
                <button type="submit" class="btn btn-success">💾 Lưu thay đổi</button>
              </div>
            </form>
          </div>
        </div>
        <!-- End Card -->
      </div>
    </div>
  </div>

  <!-- MDB JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.2.0/mdb.min.js"></script>
</body>
</html>
