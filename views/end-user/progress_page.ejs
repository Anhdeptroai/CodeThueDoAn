<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Theo dõi tiến độ đồ án</title>
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet"/>
  <!-- MDB -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.css" rel="stylesheet"/>
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f8f9fa;
    }
    .progress-container {
      max-width: 800px;
      margin: auto;
    }
    .step {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
    }
    .step .icon {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #fff;
      margin-right: 15px;
    }
    .step.done .icon { background: #198754; } /* xanh - done */
    .step.doing .icon { background: #0d6efd; } /* xanh dương - doing */
    .step.todo .icon { background: #6c757d; } /* xám - todo */
  </style>
</head>
<body>


<!-- Header -->
  <%- include('partials/header') %>


<!-- Nội dung -->
<div class="container py-5 progress-container">
  <h3 class="fw-bold mb-3 text-center">Dự án: Website Quản lý Sinh viên</h3>
  <p class="text-center text-muted">Deadline: 30/09/2025</p>

  <!-- Tổng tiến độ -->
  <div class="mb-5">
    <h5>Tiến độ tổng thể: <span class="text-primary fw-bold">50%</span></h5>
    <div class="progress" style="height: 25px;">
      <div class="progress-bar progress-bar-striped progress-bar-animated bg-info" role="progressbar" style="width: 50%">
        50%
      </div>
    </div>
  </div>

  <div>
    <% if (progresslist && progresslist.length > 0) { %>
      <% progresslist.forEach(progress => { %>
        <% if (user.id === progress.user_id || user.role === 1) { %>
          <div class="step done">
            <div class="icon"><i class="<%= progress.status == 0 
                                            ? 'fas fa-clock' 
                                            : (progress.status == 1 
                                                ? 'fas fa-spinner fa-spin' 
                                                : 'fas fa-check') %>"></i></div>
            <div>
              <h6 class="fw-bold mb-0"><%= progress.title_progress %></h6>
              <% if (progress.status == 0) { %>
                <small class="text-muted">Chưa bắt đầu</small>
              <% } else if (progress.status == 1) { %>
                <small class="text-primary">Đang thực hiện (<%= progress.percent %>%)</small>
              <% } else { %>
                <small class="text-success">Hoàn thành</small>  
              <% } %>
            </div>
          </div>
        <% } %>
      <% } ) %>
    <% } else { %>
      <tr>
          <td colspan="5">Không có dữ liệu</td>
      </tr>
    <% } %>
  </div>

</div>

<!-- Footer -->
  <%- include('partials/footer') %>

<!-- MDB JS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.js"></script>
</body>
</html>
